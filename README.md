# RecyclerView
App RecyclerView in Android Studio 3.6.3

If the app needs to display a scrolling list of elements based on large data sets (or data that changes frequently), use RecyclerView or CardView.
